- name: Setup Blackbox
  hosts: localhost
  become: yes
  vars_files:
    - ../vars/main.yml
  
  roles:
    - { role: base, tags: ['base'] }
    - { role: windowmanager, tags: ['windowmanager'] }

  tasks:
    - name: Final system configuration
      debug:
        msg: "Blackbox Setup Complete!"
