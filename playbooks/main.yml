- name: Setup Blackbox
  hosts: localhost
  become: yes
  vars_files:
    - ../vars/main.yml
  
  roles:
    - { role: base-system, tags: ['base-system'] }
    - { role: sway-desktop, tags: ['sway-desktop'] }
    - { role: security-hardening, tags: ['security-hardening'] }
